import genAwait from "../../../../components/getAwait";

export default async function Page() {
  // const data = await genAwait(7);

  // const newData = await genAwait(5);

  return (
    <>
      <h1>Profile</h1>
    </>
  );
}
